# 简化安全方案 - 额度管理

## 🎯 设计理念

基于你的需求，采用**简化但有效**的安全方案：

**核心理念**：
- ✅ 不限制分享（用户可以随意分享播放列表）
- ✅ 不检测代理行为（信任用户）
- ✅ 仅按正常使用行为控制额度
- ✅ 超限自动封禁，防止滥用

**优势**：
- 🎉 用户体验好：无需频繁刷新订阅
- 💡 实现简单：只需额度计数
- 🛡️ 防滥用有效：每个频道100次/天
- 🤖 自动化：超限自动封禁

## 📊 额度规则

### 基本规则
```
每个频道：每天 100 次播放额度
独立计算：每个频道额度独立
重置时间：每天凌晨0点自动重置
```

### 使用场景分析

**正常用户**：
```
假设行为：
- 每天看10个不同的频道
- 每个频道播放10次（来回切换）
- 总计：10 × 10 = 100次/频道

结果：✅ 完全够用，不会超限
```

**重度用户**：
```
假设行为：
- 每天看20个不同的频道
- 每个频道播放20次
- 总计：20 × 20 = 400次/频道

结果：⚠️ 超限，会触发封禁
```

**滥用者**：
```
假设行为：
- 使用脚本或代理刷播放地址
- 短时间访问单个频道 > 100次
- 耗尽额度触发封禁

结果：❌ 自动封禁，无法继续滥用
```

## 🔒 自动封禁机制

### 触发条件
1. **单个频道超过100次播放**
2. **当天内触发**
3. **自动执行**

### 封禁动作
```
1. 将卡密状态改为 'disabled'
2. 在备注中添加封禁原因
3. 记录封禁时间到KV
4. 拒绝所有播放请求（返回403）
```

### 封禁原因格式
```
系统自动封禁：超出每日播放额度（每个频道100次） (2025-12-25)
```

## 📱 播放地址格式

### 旧格式（当前使用的）
```
https://your-domain.com/live/{code}/{channel_hash}
```

**特点**：
- ✅ 固定格式，兼容所有播放器
- ✅ 无需频繁刷新订阅
- ✅ 正常使用无感知影响

### 优点对比

| 特性 | 令牌方案 | 额度方案 |
|------|----------|----------|
| 播放地址 | 动态令牌 | 固定地址 |
| 需要刷新 | 频繁 | 不需要 |
| 用户体验 | 较差 | 很好 |
| 防分享 | 强 | 宽松 |
| 防批量 | 强 | 有效 |
| 实现复杂 | 高 | 低 |

## 🎛️ 管理功能

### 1. 额度查询

**API接口**：
```
GET /admin/security/quota?code={卡密}
```

**返回数据**：
```json
{
  "success": true,
  "date": "2025-12-25",
  "total_plays": 50,
  "exceeded_channels_count": 0,
  "is_banned": false,
  "banned_at": null,
  "details": {
    "totalPlays": 50,
    "exceededChannels": [],
    "isBanned": false
  }
}
```

**前端功能**：
- 查看今日播放总次数
- 查看超限频道数
- 查看封禁状态
- 查看封禁时间

### 2. 手动解封

**API接口**：
```
POST /admin/security/unban
Body: {
  "code": "卡密"
}
```

**功能**：
- 将卡密状态改为 'active'
- 清除封禁备注
- 清除额度记录（可选）
- 恢复正常使用

**操作步骤**：
1. 输入卡密
2. 点击"查询额度"
3. 查看是否已封禁
4. 如是，点击"解封卡密"
5. 确认解封

### 3. 自动解封

**机制**：
- 每天凌晨0点自动重置额度
- 额度重置后，用户可以正常使用
- **不会自动解封**，需要管理员手动解封

**设计原因**：
- 防止用户继续滥用
- 管理员需要确认解封
- 给用户警告和反馈机会

## 🔧 参数配置

### 安全配置（handlers/live.js）

```javascript
const SECURITY_CONFIG = {
  // 每个频道每日播放次数限制
  CHANNEL_DAILY_LIMIT: 100,

  // 超限后的自动封禁
  AUTO_BAN_ON_EXCEED: true,

  // 封禁原因备注
  BAN_REASON: "系统自动封禁：超出每日播放额度（每个频道100次）"
};
```

### 调整建议

**更严格**：
```javascript
CHANNEL_DAILY_LIMIT: 50,  // 降低到50次
```

**更宽松**：
```javascript
CHANNEL_DAILY_LIMIT: 200, // 提高到200次
```

**禁用自动封禁**：
```javascript
AUTO_BAN_ON_EXCEED: false, // 只限制不封禁
```

## 📊 成本估算

### KV存储

**1000个活跃卡密，每人看100个频道/天**：

| 项目 | 数量/天 | 大小 | 成本/天 |
|------|----------|------|----------|
| 频道额度计数 | 100,000 | ~5MB | $0.0025 |
| 卡密汇总记录 | 1,000 | ~100KB | $0.00005 |
| IP并发记录 | 1,000 | ~100KB | $0.00005 |
| **总计** | - | - | **~$0.0026** |

**月成本**：约 $0.08

### 对比令牌方案

| 方案 | 成本/月 | 存储/天 |
|------|----------|----------|
| 令牌方案 | ~$0.66 | ~20MB |
| **额度方案** | **~$0.08** | **~5MB** |

**成本降低**：88% 💰

## 🚀 部署步骤

### 1. 部署Worker
```bash
cd c:/Users/60270/Desktop/cfworker2
wrangler deploy
```

### 2. 验证部署
```bash
curl -I https://sys.iptv-search.com/admin/init \
  -H "X-Admin-Key: your-admin-key"
```

### 3. 测试额度功能

```bash
# 1. 获取订阅
curl https://sys.iptv-search.com/sub/YOUR_CODE.m3u

# 2. 查看播放地址格式
grep "live/" playlist.m3u
# 应该看到：/live/YOUR_CODE/HASH（无令牌）

# 3. 查询额度
curl https://sys.iptv-search.com/admin/security/quota?code=YOUR_CODE \
  -H "X-Admin-Key: your-admin-key"
```

## 📋 管理员工作流程

### 日常监控
```
每天查看：
1. 访问"安全监控"页面
2. 输入卡密查询额度
3. 关注封禁状态的卡密
4. 记录频繁超限的用户
```

### 处理封禁
```
发现封禁后：
1. 查看封禁原因和备注
2. 确认是否为误封
3. 如果是误封 → 手动解封
4. 如果是滥用 → 联系用户或保持封禁
```

### 额度优化
```
定期分析：
1. 观察用户使用模式
2. 分析超限频率
3. 必要时调整额度值
4. 平衡安全和体验
```

## ⚠️ 用户通知

### 建议告知内容

```
通知用户额度规则：

📺 电视直播服务使用说明

✅ 正常观看，无需频繁刷新订阅
📊 每个频道每天100次播放额度
🔄 额度每天凌晨0点自动重置
⚠️ 超过额度会自动封禁
🔓 封禁后需联系管理员解封

正常使用完全够用，请放心观看！
```

### 封禁通知

```
封禁后提示：

⚠️ 您的卡密已被封禁

原因：超出每日播放额度

今日播放次数：XXX次
超限频道数：X个

解决方法：
1. 等待次日额度重置（凌晨0点）
2. 如为误封，联系客服处理

感谢您的理解与支持！
```

## 🔍 故障排除

### Q1: 为什么会被封禁？
A: 单个频道当天播放超过100次。
可能是：
- 使用脚本刷播放
- 代理服务器批量访问
- 网络故障重试频繁

### Q2: 如何解封？
A:
1. 联系管理员
2. 管理员登录后台
3. 输入卡密查询额度
4. 点击"解封卡密"
5. 等待次日额度重置

### Q3: 次日会自动解封吗？
A: 不会。
设计原因：
- 防止用户继续滥用
- 需要管理员确认
- 给用户反馈机会

### Q4: 额度为什么是0？
A: 可能是：
- 刚过凌晨0点，额度刚重置
- 该卡密今天还没有播放记录
- 管理员重置了额度

### Q5: 如何提高额度？
A: 编辑 `handlers/live.js`：
```javascript
CHANNEL_DAILY_LIMIT: 200, // 提高到200次
```

然后重新部署。

## 📈 未来改进

### 1. 灵活额度
- 根据卡密类型设置不同额度
- VIP用户：500次/天
- 普通用户：100次/天
- 试用用户：50次/天

### 2. 警告机制
- 达到80%额度时发送提醒
- 给用户缓冲时间
- 减少误封情况

### 3. 历史记录
- 记录每日额度使用情况
- 分析用户行为趋势
- 识别异常模式

### 4. 白名单
- 可信用户加入白名单
- 不检查额度限制
- 减少误报

### 5. 自助解封
- 用户可自助申请解封
- 管理员审核后处理
- 提高管理效率

## 🎉 总结

### 核心优势

✅ **用户体验优先**：无需频繁刷新订阅
✅ **实现简单**：只需额度计数
✅ **成本极低**：KV成本降低88%
✅ **防滥用有效**：100次/频道/天足够正常使用
✅ **自动化管理**：超限自动封禁
✅ **灵活可控**：参数可调整，支持手动解封

### 与令牌方案对比

| 维度 | 令牌方案 | 额度方案 |
|------|----------|----------|
| 用户体验 | ⚠️ 需要刷新 | ✅ 无需刷新 |
| 安全性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 实现复杂 | ⭐⭐⭐⭐⭐⭐ | ⭐ |
| KV成本/月 | $0.66 | $0.08 |
| 防分享 | ⭐⭐⭐⭐⭐⭐ | ⭐⭐ |
| 防批量 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

### 推荐使用

**个人用户**：
- 额度方案完美适合
- 正常使用无感知
- 100次/频道完全够用

**公开服务**：
- 可配合其他安全措施
- 考虑提高IP并发限制
- 添加WAF规则过滤

---

**部署时间**：2025-12-25
**版本**：v2.0 - 简化额度管理
