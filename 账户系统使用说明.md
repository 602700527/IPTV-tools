# 账户系统使用说明

## 功能概述

本系统已实现完整的用户账户功能，包括：

### 用户端功能
- ✅ 邮箱注册（邮箱作为用户名）
- ✅ 邮箱验证（6位数字验证码，10分钟有效期）
- ✅ 用户登录/登出
- ✅ 账户信息查看
- ✅ 订单历史查看

### 管理端功能
- ✅ 用户管理（查看、启用/禁用验证、删除）
- ✅ 订单管理（查看订单列表）
- ✅ 用户搜索和分页
- ✅ 订单筛选和分页

---

## 配置说明

### 1. 环境变量（已配置）

以下环境变量已添加到 `wrangler.toml`：

```toml
[vars]
RESEND_API_KEY = "re_SV65wtWn_6zDFiwGvPFRRv3gbcLDACDKe"
FROM_EMAIL = "support@iptv-search.com"
APP_URL = "https://iptv-search.com"
```

### 2. 邮件服务

系统使用 **Resend** 发送验证邮件：

- **服务商**: Resend
- **API Key**: `re_SV65wtWn_6zDFiwGvPFRRv3gbcLDACDKe`
- **发件邮箱**: `support@iptv-search.com`
- **免费额度**: 每天 3,000 封邮件

### 3. 数据库表结构

已创建以下表：

- `users` - 用户表（邮箱、密码哈希、验证状态）
- `email_verifications` - 邮箱验证码表
- `user_sessions` - 用户会话表
- `user_orders` - 用户订单表

---

## API 接口说明

### 用户端 API

#### 1. 注册
```
POST /api/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**响应**:
```json
{
  "success": true,
  "message": "注册成功，请验证邮箱"
}
```

#### 2. 发送验证码
```
POST /api/auth/send-code
Content-Type: application/json

{
  "email": "user@example.com"
}
```

**响应**:
```json
{
  "success": true,
  "message": "验证码已发送到您的邮箱"
}
```

#### 3. 验证邮箱
```
GET /api/auth/verify?email=user@example.com&code=123456
```

**响应**:
```json
{
  "success": true,
  "message": "邮箱验证成功"
}
```

#### 4. 登录
```
POST /api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**响应**:
```json
{
  "success": true,
  "message": "登录成功",
  "token": "session_token_here",
  "user": {
    "id": 1,
    "email": "user@example.com",
    "is_verified": true,
    "created_at": "2024-01-01T00:00:00.000Z"
  }
}
```

#### 5. 登出
```
POST /api/auth/logout
Authorization: Bearer {token}
```

#### 6. 获取用户信息
```
GET /api/auth/user
Authorization: Bearer {token}
```

#### 7. 获取订单历史
```
GET /api/auth/orders
Authorization: Bearer {token}
```

### 管理端 API（需要 Admin Key）

#### 1. 获取用户列表
```
GET /admin/users?page=1&pageSize=20&search=user@
X-Admin-Key: {ADMIN_KEY}
```

#### 2. 更新用户验证状态
```
PUT /admin/users
Content-Type: application/json
X-Admin-Key: {ADMIN_KEY}

{
  "id": 1,
  "is_verified": true
}
```

#### 3. 删除用户
```
DELETE /admin/users?id=1
X-Admin-Key: {ADMIN_KEY}
```

#### 4. 获取订单列表
```
GET /admin/orders?page=1&pageSize=20&userId=1
X-Admin-Key: {ADMIN_KEY}
```

---

## 页面路由

### 用户页面
- `/account` - 用户中心（账户信息、订单历史）

### 管理后台
- `/admin` - 管理后台（新增"账户管理"和"订单管理"标签）

---

## 使用流程

### 用户注册流程

1. **注册账户**
   - 访问首页，点击"登录"按钮
   - 点击"立即注册"
   - 输入邮箱和密码（至少8位）

2. **验证邮箱**
   - 注册成功后自动跳转到验证页面
   - 输入6位数字验证码（邮箱中接收）
   - 验证码10分钟内有效

3. **登录系统**
   - 验证完成后使用邮箱和密码登录
   - 登录成功后可访问用户中心

### 管理员操作流程

1. **访问管理后台**
   - 打开 `/admin` 页面
   - 输入 Admin Key 登录

2. **用户管理**
   - 点击"账户管理"标签
   - 查看用户列表
   - 搜索用户（支持邮箱模糊搜索）
   - 启用/禁用用户邮箱验证
   - 删除用户

3. **订单管理**
   - 点击"订单管理"标签
   - 查看订单列表
   - 按用户邮箱筛选订单

---

## 安全特性

### 密码安全
- 密码长度至少 8 个字符
- 使用 SHA-256 哈希存储（非明文）
- 不在前端存储密码

### 验证码安全
- 6 位随机数字验证码
- 10 分钟有效期
- 验证成功后自动标记为已使用
- 1 分钟内只能发送一次验证码

### 会话管理
- 生成随机 32 字节会话令牌
- 会话有效期 30 天
- 支持登出销毁会话

### 防护措施
- 邮箱格式验证
- 登录失败不返回具体错误
- SQL 注入防护（参数化查询）
- XSS 防护（HTML 转义）

---

## 邮件模板

验证邮件包含：
- 邮件主题：邮箱验证 - TV Live Service
- 6 位数字验证码（醒目显示）
- 直接验证链接
- 有效期说明
- 安全提示

---

## 注意事项

1. **Resend 配置**
   - 确保在 Resend 中验证了域名 `iptv-search.com`
   - 如需使用自定义域名，需要在 DNS 中添加 TXT 记录

2. **生产环境**
   - 生产环境的 Admin Key 需要更改（当前为 `admin-key-please-change-in-production`）
   - 建议使用强密码作为 Admin Key

3. **数据库迁移**
   - 已执行迁移脚本 `003_add_user_system.sql`
   - 本地和生产数据库都已更新

4. **日志监控**
   - 建议定期查看 Cloudflare Workers 日志
   - 关注邮件发送失败、验证错误等情况

---

## 故障排查

### 邮件未收到
1. 检查 Resend 账户额度
2. 查看邮件是否进入垃圾箱
3. 检查收件地址是否正确

### 验证码无效
1. 确认验证码输入正确
2. 检查验证码是否过期（10分钟）
3. 尝试重新发送验证码

### 登录失败
1. 确认邮箱已验证
2. 检查密码是否正确
3. 查看浏览器是否保存了正确的会话令牌

---

## 未来扩展

可以考虑添加的功能：
- 找回密码功能
- 修改密码功能
- 第三方登录（Google、GitHub）
- 两步验证（2FA）
- 用户权限等级
- 订单管理（创建、取消订单）
- 支付集成
- 用户行为统计

---

## 联系方式

如有问题，请联系：
- 邮箱：support@iptv-search.com
- GitHub Issues

---

**更新时间**: 2024-01-19
