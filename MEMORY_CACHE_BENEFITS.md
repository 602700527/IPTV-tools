# 内存缓存优化性能对比

## 优化目标

减少订阅和播放请求对数据库的查询次数，提升系统性能和响应速度。

## 数据库查询次数对比

### 优化前

假设：
- 每天有 10,000 次订阅请求
- 每天有 100,000 次播放请求
- 每个请求平均查询数据库 1 次

**数据库查询次数：**
- 订阅：10,000 次/天
- 播放：100,000 次/天
- **总计：110,000 次/天**

**数据库写入次数：**
- 订阅IP记录：约 10,000 次/天（假设每个IP都是新的）

### 优化后

假设：
- 每天有 10,000 次订阅请求
- 每天有 100,000 次播放请求
- 每 10 分钟刷新一次缓存
- 每天刷新：1440 分钟 / 10 分钟 = 144 次

**数据库查询次数：**
- 订阅：0 次/天（全部在内存中）
- 播放：0 次/天（全部在内存中）
- 缓存刷新时的去重查询：约 1,000 次/天（假设平均每次刷新有 7 个新IP）
- **总计：约 1,000 次/天**

**数据库写入次数：**
- 订阅IP记录：约 1,000 次/天（去重后的实际新IP）

## 性能提升

### 数据库查询减少

- 优化前：110,000 次/天
- 优化后：1,000 次/天
- **减少：99.1%**

### 响应时间提升

- 优化前：每次请求需要查询数据库（约 10-50ms）
- 优化后：全部在内存中（约 0.1-1ms）
- **提升：10-50 倍**

### 数据库压力减少

- 优化前：每个请求都查询数据库
- 优化后：每 10 分钟才批量写入一次
- **压力减少：99.1%**

## 成本节约

假设 Cloudflare D1 定价（参考）：
- 读取：$0.000001/次
- 写入：$0.00001/次

### 优化前成本

**读取成本：**
- 110,000 次/天 × $0.000001 = $0.11/天

**写入成本：**
- 10,000 次/天 × $0.00001 = $0.10/天

**总计：$0.21/天 = $6.30/月**

### 优化后成本

**读取成本：**
- 1,000 次/天 × $0.000001 = $0.001/天

**写入成本：**
- 1,000 次/天 × $0.00001 = $0.01/天

**总计：$0.011/天 = $0.33/月**

### 节约成本

- $6.30/月 - $0.33/月 = **$5.97/月**
- **节约：94.8%**

## 内存占用估算

### 每个卡密的缓存占用

假设：
- 每个卡密平均有 3 个IP（max_ips = 3）
- 每个 IP 字符串平均 15 字节
- 时间戳：8 字节
- Map/Set 开销：约 50 字节/项

**单个卡密占用：**
- subscriptionIPCache: 50 字节（Map entry） + 3 × (15 字节（IP） + 50 字节（Set entry）） ≈ 215 字节
- subscriptionIPTimestamp: 3 × (约 50 字节（Map entry） + 8 字节（timestamp）） ≈ 174 字节
- **总计：约 389 字节/卡密**

### 总内存占用

假设：
- 活跃卡密数量：1,000 个
- 30分钟窗口期

**总占用：**
- 389 字节 × 1,000 = 389 KB
- **实际占用：约 400 KB**

**结论：内存占用非常小，对性能几乎没有影响。**

## 高并发场景

### 场景：1,000 个用户同时订阅

**优化前：**
- 数据库查询：1,000 次
- 响应时间：10-50ms × 1,000 = 10-50 秒（串行）
- 数据库压力：极大，可能导致超时

**优化后：**
- 数据库查询：0 次
- 响应时间：0.1-1ms × 1,000 = 0.1-1 秒（串行）
- 数据库压力：无，可以轻松应对

## 可靠性保障

### 数据持久化

1. **KV 备份**：
   - 每 10 分钟备份一次
   - 包含所有订阅IP和播放计数
   - 过期时间：11 分钟

2. **数据库存储**：
   - 每 10 分钟批量写入一次
   - 去重后只写入新数据
   - 可用于历史记录和数据分析

3. **Worker 重启恢复**：
   - 从 KV 恢复缓存（最多丢失 10 分钟数据）
   - 从数据库恢复（如果有需要）

### 容错机制

- 如果 KV 写入失败，只影响备份，不影响服务
- 如果数据库写入失败，只影响历史记录，不影响当前服务
- 内存缓存独立运行，不依赖外部服务

## 总结

### 优化效果

✅ **数据库查询减少 99.1%**
✅ **响应时间提升 10-50 倍**
✅ **成本节约 94.8%**
✅ **内存占用仅 400 KB（1,000 卡密）**
✅ **可轻松应对高并发场景**

### 适用场景

✅ 订阅请求频繁的场景
✅ 播放请求频繁的场景
✅ 高并发的场景
✅ 需要快速响应的场景

### 不适用场景

❌ 需要实时历史记录的场景（因为数据每10分钟才写入数据库）
❌ 需要跨Worker共享数据的场景（每个Worker实例有独立的内存）

### 建议配置

- **缓存刷新间隔**：10 分钟（默认）
- **IP有效期**：30 分钟（根据业务需求调整）
- **max_ips**：3（根据业务需求调整）
- **KV 过期时间**：11 分钟（缓存刷新间隔 + 1 分钟）
