# 域名黑名单功能说明

## 功能概述

域名黑名单功能允许管理员指定某些域名,这些域名的直播源将不会被Cloudflare代理,而是直接将原始播放地址透传给用户。这对于禁止Cloudflare IP访问的直播源非常有用。

## 数据库结构

新增表 `domain_blacklist`:

```sql
CREATE TABLE IF NOT EXISTS domain_blacklist (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  domain TEXT NOT NULL UNIQUE,
  reason TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

## 使用方法

### 1. 访问管理后台

登录管理后台,点击"域名黑名单"标签页。

### 2. 添加域名到黑名单

- **单个域名**: 在"添加域名到黑名单"部分,输入一个域名,例如: `example.com`
- **批量添加**: 每行输入一个域名,例如:
  ```
  example.com
  *.example.com
  cdn.example.com
  ```
- **备注**: 可选,用于记录添加原因

### 3. 域名格式

- **完全匹配**: `example.com` - 只匹配 example.com
- **子域名匹配**: `*.example.com` - 匹配所有 example.com 的子域名,如 cdn.example.com, video.example.com 等

### 4. 删除域名

在域名黑名单列表中,点击"删除"按钮即可从黑名单中移除域名。

## 工作原理

### 订阅M3U生成

1. **付费订阅** (`/sub/{code}.m3u`):
   - 系统加载域名黑名单
   - 遍历所有频道,检查播放地址的域名是否在黑名单中
   - 如果域名在黑名单中,直接返回原始播放地址(透传)
   - 如果域名不在黑名单中,返回代理地址 `/live/{code}/{hash}`

2. **免费订阅** (`/api/freesub/{subId}.m3u`):
   - 同样支持域名黑名单透传
   - 黑名单域名返回原始URL
   - 非黑名单域名返回 `/api/play/{channel_hash}?freesub={subId}`

### 播放流程

**黑名单域名的播放流程:**
```
用户请求 -> M3U生成 -> 返回原始URL -> 用户直接访问原始源
```

**非黑名单域名的播放流程:**
```
用户请求 -> M3U生成 -> 返回代理地址 -> 用户请求代理地址 -> 服务器重定向到真实URL
```

## 应用场景

1. **直播源拒绝CF访问**: 某些直播源会检测并拒绝来自Cloudflare IP的访问
2. **需要直接访问**: 某些直播源需要客户端直接访问以获取正确的地理位置信息
3. **避免代理失败**: 避免因代理导致的播放失败或限流问题

## 注意事项

1. **透传URL的安全性**: 黑名单域名的原始URL会直接暴露给用户,请确保这些URL是安全的
2. **性能考虑**: 黑名单域名无法享受Cloudflare的CDN加速和缓存
3. **缓存更新**: 修改域名黑名单后,订阅M3U的缓存时间(通常5-12小时)内可能不会立即生效

## API接口

### 获取域名黑名单列表

```
GET /admin/domain-blacklist
Headers: X-Admin-Key: your-admin-key
```

### 添加单个域名

```
POST /admin/domain-blacklist
Headers: 
  X-Admin-Key: your-admin-key
  Content-Type: application/json
Body:
{
  "domain": "example.com",
  "reason": "拒绝CF访问"
}
```

### 批量添加域名

```
POST /admin/domain-blacklist
Headers: 
  X-Admin-Key: your-admin-key
  Content-Type: application/json
Body:
{
  "domains": [
    {
      "domain": "example.com",
      "reason": "拒绝CF访问"
    },
    {
      "domain": "*.example.com",
      "reason": "所有子域名"
    }
  ]
}
```

### 删除域名

```
DELETE /admin/domain-blacklist/{id}
Headers: X-Admin-Key: your-admin-key
```

## 相关文件

- 数据库迁移: `migrations/007_add_domain_blacklist.sql`
- 数据库函数: `database.js` (getDomainBlacklist, isDomainBlacklisted, addDomainToBlacklist, removeDomainFromBlacklist, addMultipleDomainsToBlacklist, extractDomainFromUrl)
- API接口: `handlers/admin.js` (domain-blacklist case)
- 管理界面: `admin-page.js` (域名黑名单Tab和相关函数)
- 订阅生成: `handlers/sub.js` (支持黑名单透传)
- 免费订阅: `handlers/freesub-api.js` (支持黑名单透传)
- M3U生成: `database.js` (generateM3UContent函数支持domainBlacklist参数)
